{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <br><br>
            <div class="card">
                <div class="card-header">Editar Post</div>
                <div class="card-body">
                    {{ include('post/_form.html.twig', {'button_label': 'Actualizar'}) }}
                </div>
                <div class="card-footer">
                    <div class="btn-group">
                        <a href="{{ path('post_index') }}" class="btn btn-dark">Regresar A La Lista</a>

                        {{ include('post/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
